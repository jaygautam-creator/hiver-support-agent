# Google AI Studio API key (free tier): https://aistudio.google.com/apikey
# Only needed for `make repro-live`. `make repro` replays the committed cache
# and needs no key at all.
GOOGLE_API_KEY=...

# Optional model overrides. Defaults are set in src/config.py:
#   AGENT_MODEL=gemini-3.1-flash-lite
#   JUDGE_MODEL=gemini-3.5-flash-lite
